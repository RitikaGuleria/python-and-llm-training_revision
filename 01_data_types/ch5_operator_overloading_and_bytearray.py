base = ["water","milk","sugar"]
flavor = ['vanilla']
mix = base + flavor
print(mix)

brew = base*3
print(brew)

# bytearray: Return a new array of bytes. bytearray is a mutable sequence of integers in the range 0 <= x < 256. It is defined using bytearray() constructor.