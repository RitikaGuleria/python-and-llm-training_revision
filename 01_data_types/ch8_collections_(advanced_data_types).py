""" 
using or import third party code or others code or module in our code
datetime, time,calendar, timedelta, arrow, dateutil

from collections import namedtuple,deque
riya = namedtuple("riya","[name:"riya"]")

collection is a big thing in python, collections is a built-in Python module that provides useful data structures., it is a module which has many data types like namedtuple, deque, counter, orderedDict, defaultDict, chainMap, etc.
"""

# This is technically a multi-line string, not a comment. Python simply doesn't use it if it's not assigned anywhere, but it's better to use # for actual comments.